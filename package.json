{
  "name": "vocal-route-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "demo:dev": "yarn workspace @vocalroute/demo-web dev",
    "sdk:build": "yarn workspace vocalroute-sdk build",
    "backend:dev": "yarn workspace @vocalroute/backend dev",
    "lint": "yarn workspaces run lint",
    "format": "yarn workspaces run format"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "typescript": "^5"
  }
}