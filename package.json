{
  "name": "vocal-route-monorepo",
  "private": true,
  "workspaces": [
    "packages/sdk"
  ],
  "scripts": {
    "sdk:build": "yarn workspace @vocalroute-ai/sdk build",
    "lint": "yarn workspaces run lint",
    "format": "yarn workspaces run format"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "typescript": "^5"
  }
}